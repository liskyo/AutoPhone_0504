# 自動拍照系統（AutoPhote）新人培訓教師手冊

> 適用對象：新進軟體工程師（Python / 桌面系統 / 工業相機整合）  
> 建議課程型態：2 天工作坊（可拆為 8 堂 60–90 分鐘）  
> 教學目標：讓新人可在短時間內理解系統架構、能獨立除錯、可執行小型功能開發與交付

---

## 教師使用說明

- 本手冊以「先懂流程、再看模組、最後動手改」為主線。
- 每章都包含：**章節目標、授課重點、示範操作、實作任務、驗收標準、常見卡點**。
- 若學員程度不一，建議採「核心必修 + 進階選修」：
  - 必修：第 1–6 章
  - 進階：第 7–8 章

---

## 章節總覽（8 章）

1. 系統全貌與業務流程
2. 專案結構與啟動生命週期
3. 設定系統與環境切換（真機/模擬）
4. 拍照核心流程（CaptureManager）
5. 海康 SDK 與硬體整合（HikCamera）
6. UI 操作流與執行緒安全
7. NG 錯誤排除與效能調校
8. 打包部署與交付演練

---

## 第 1 章：系統全貌與業務流程

### 章節目標

- 讓學員知道系統在產線中的角色與價值。
- 建立「START → REVIEW → CONFIRM → Upload」完整心智模型。

### 授課重點

- AutoPhote 是多相機拍照工作站，不是單純拍照程式。
- 操作流程：
  - `START`：觸發拍照並暫存（不立即落檔）
  - `CONFIRM`：正式存檔 + 進入上傳佇列
  - `RETAKE`：捨棄暫存結果
- 背景上傳與前台操作解耦，降低操作卡頓。

### 教師示範（10–15 分）

- 用「操作員情境」開場：假設現場要拍 5 台機台，教師先示範從輸入 SN 到確認存檔的完整流程。
- 示範時同步口述每一步的系統狀態變化：
  - 按 `START` 前：相機應為 READY。
  - 按 `START` 後：狀態會進入 GO/CHECK（依流程）。
  - 按 `CONFIRM` 後：檔案寫入、佇列計數變動、狀態轉為 OK。
- 示範一次 `RETAKE` 分支，強調「暫存影像會被捨棄，未確認不落檔」。
- 在畫面上直接指出相機狀態燈號（READY/GO/OK/NG/CHECK）與實際意義，避免學員只背英文縮寫。
- 示範結束後，請學員用 1 分鐘復述流程，教師立即修正錯誤心智模型。

### 實作任務（學員）

- 任務 1：2 人一組在白板畫出資料流，必須包含 `UI -> Capture -> FileService -> Queue -> Upload`。
- 任務 2：在流程圖標出 3 個關鍵決策點：
  - 為何 `START` 不直接存檔。
  - 何時影像進入 `pending_captures`。
  - 何時才會進入 upload queue。
- 任務 3：每組提出 1 個「若流程設計錯誤會造成什麼現場風險」的例子（例如誤存、漏存、誤覆蓋）。

### 驗收標準

- 能正確口述完整操作流（START/RETAKE/CONFIRM 三分支）且無邏輯矛盾。
- 能說明 `pending` 與「已落檔」的差異，並指出對應 UI 行為。
- 能清楚解釋 queue 的用途：解耦拍照流程與背景複製流程，避免前台阻塞。
- 流程圖需至少包含 5 個元件與 6 條有方向的連線，且符號命名一致。

**參考答案（教師用）**
- 正確流程：輸入 SN -> `START` 觸發拍照並進 `pending` -> 可 `RETAKE` 丟棄 -> `CONFIRM` 才存檔並入 queue -> `UploadManager` 背景複製到遠端。
- `pending` 是記憶體中的待確認影像，不保證磁碟存在；「已落檔」是 `FileService.save_image()` 成功後已有實體檔案路徑。
- queue 的正解關鍵字：非同步、解耦、削峰、避免 UI/拍照被上傳阻塞。
- 流程圖最低合格元素建議：`DashboardApp`、`CaptureManager`、`FileService`、`upload_queue`、`UploadManager`、`REMOTE_SERVER_STORAGE`。

### 常見卡點（教師提示）

- 學員容易以為「上傳」是 HTTP；本系統實作是 copy 到遠端路徑。
- 學員常把「預覽看到」誤判為「已存檔」，可用 `RETAKE` 示範反證。
- 容易忽略 SN 對命名規則的影響，後續章節可回扣檔名格式做驗證。

---

## 第 2 章：專案結構與啟動生命週期

### 章節目標

- 能快速定位需求應修改的檔案。
- 理解 `main.py` 如何組裝整個系統。

### 授課重點

- 目錄職責：
  - `ui/`：介面與事件
  - `services/`：業務流程（拍照、上傳、檔案）
  - `hardware/`：實體相機/模擬相機
  - `config.py`：設定載入與套用
- `main.py` 流程：
  - 建立資料夾
  - 建立共享 queue
  - 建立 `CaptureManager` / `UploadManager`
  - 建立 UI 與 callback 注入
  - 啟動預覽與上傳執行緒
  - 關閉時資源釋放

### 教師示範

- 帶學員走讀 `main.py`，以「啟動序」與「關閉序」兩條線解說，而不是逐行念程式。
- 示範 callback 注入關係：`DashboardApp` 只拿到 `on_snap/on_confirm/on_retake`，降低模組耦合。
- 示範若把 `initialize_cameras()` 延後到 `mainloop()` 後會造成的問題（無法在進入 UI 前完成相機狀態初始化）。
- 示範 `on_close` 為何必須先 shutdown capture 再 stop upload，避免資源未釋放或程序殘留。

### 實作任務

- 任務 1：畫出 `main.py` 啟動 sequence（至少 8 個節點）。
- 任務 2：標出「若順序錯誤會壞掉」的 3 個點（例如先用 app 再建 app）。
- 任務 3：每位學員用 3 句話描述：
  - 系統最早建立哪個共享資源。
  - UI 如何回呼到服務層。
  - 關閉時哪個步驟最不能省略。

### 驗收標準

- 能正確解釋 `tk.Tk()`、`DashboardApp`、callback 三者的建立先後依賴。
- 能說出 `capture_mgr.shutdown()` 至少兩個目的（停預覽與釋放相機/執行緒資源）。
- 能從架構角度說明 queue 為什麼在 `main.py` 集中建立，而不是在各模組各建一份。

**參考答案（教師用）**
- 先後關係標準答法：先建 `tk.Tk()`（UI root）-> 建服務物件 -> 建 `DashboardApp` 並注入 callback；否則 UI 元件與事件綁定無法正確建立。
- `capture_mgr.shutdown()` 至少包含：停止預覽執行緒、相機 disconnect、thread pool shutdown（避免殘留資源）。
- queue 由 `main.py` 建立的理由：單一共享管線、依賴注入清楚、方便測試與替換，不會出現「多個 queue 各自為政」。

### 常見卡點（教師提示）

- 學員會把 `main.py` 當成「業務邏輯中心」，要提醒它其實是組裝層。
- 容易忽略關閉流程，導致測試時出現殘留程序或裝置未釋放。

---

## 第 3 章：設定系統與環境切換（真機/模擬）

### 章節目標

- 學會調整 `settings.json` 與理解哪些設定可即時生效。
- 會切換真機與 mock 環境做開發。

### 授課重點

- `config.py`：
  - `DEFAULT_SETTINGS`
  - `load_settings()` / `save_settings()` / `reload_settings()`
  - `USE_REAL_CAMERA` 切換
- 可即時套用：stagger、grab timeout、品質、縮放、路徑。
- 需重啟：相機數量、IP（涉及重新連線與 UI 網格重建）。

### 教師示範

- 示範在設定頁調整 `Stagger` 與 `Grab timeout`，立即做 5 次連拍觀察差異。
- 示範修改 `Resize`、`Quality`，讓學員觀察存檔速度與檔案大小變化。
- 切換 `USE_REAL_CAMERA` 為 `False`，在無實機環境完成一次完整流程。
- 示範 `settings.json` 實際寫入位置（source 模式與 exe 模式差異）。

### 實作任務

- 任務 1：設計兩組設定：
  - 穩定優先（低 NG、可接受速度）
  - 速度優先（較快節拍、可接受風險）
- 任務 2：以表格記錄每組設定的預期效果與風險。
- 任務 3：每組做 10 次連拍，記錄 NG 次數與平均完成時間。

### 驗收標準

- 能解釋哪些設定可即時生效、哪些設定需重啟且原因正確。
- 能用測試結果支持自己的設定選擇，而非只憑感覺。
- 能描述 `reload_settings()` 的作用邊界（更新參數，不重建既有連線與版面）。

**參考答案（教師用）**
- 即時生效：`capture_stagger_ms`、`grab_frame_timeout_ms`、`resize_ratio`、`jpeg_quality`、路徑類設定。
- 需重啟：`camera_count`、`camera_ips`（原因：涉及相機重新列舉/連線與 UI 網格重建）。
- `reload_settings()` 正解：更新 `config` 模組變數；不主動重建既有物件（相機連線、UI 版面）所以不能等同完整重啟。

### 常見卡點（教師提示）

- 常把「設定有寫入檔案」誤當成「系統已套用」；要要求立即驗證。
- 容易同時改太多參數，導致無法判斷效果來源。

---

## 第 4 章：拍照核心流程（CaptureManager）

### 章節目標

- 理解拍照流程的並行模型與資料生命週期。
- 能安全修改命名規則、存檔策略或批次流程。

### 授課重點

- `initialize_cameras()`：固定長度 camera slot，確保索引與 UI 對齊。
- `trigger_batch_capture(save_now=False)`：平行提交拍照任務。
- `_capture_task()`：
  - stagger 延遲
  - grab image
  - overlay timestamp
  - 更新 UI / pending
- `confirm_save()`：將 pending 寫檔並入 upload queue。
- 命名規則：`SN_YYYYMMDD_序號` + 民國日期資料夾與 session 子資料夾。

### 教師示範

- 示範 `trigger_batch_capture(save_now=False)` 與 `confirm_save()` 兩段式流程的資料生命週期。
- 在 `_capture_task` 加入臨時 debug log，觀察各 CAM 實際起拍時間差與是否符合 stagger。
- 示範一次 `pending_captures` 的建立、清空、確認存檔後 queue 入列的完整路徑。
- 示範 `SN` 空白時的檔名結果，帶到命名規則的防呆思維。

### 實作任務

- 任務 1：修改規則，將 SN 空白時前綴改為 `NOSN`，並保留其餘命名邏輯。
- 任務 2：自行設計 3 組測試案例（有 SN、空 SN、同日多批次）驗證檔名不衝突。
- 任務 3：輸出一段簡短變更說明（修改點、風險、回滾方法）。

### 驗收標準

- START/CONFIRM/RETAKE 流程皆可正常執行且無例外中斷。
- 檔名規則符合預期，且連拍下流水號不重複。
- 修改未影響 queue 入列與遠端複製流程。
- 提交內容包含最少 3 個可重現測試步驟與結果。

**參考答案（教師用）**
- `NOSN` 改動正確條件：僅 SN 空白時改前綴，非空 SN 不可被覆蓋。
- 檔名正確樣式：`{SN或NOSN}_{YYYYMMDD}_{001..}.jpg`，同日連拍流水號遞增且不重複。
- 回歸檢查重點：`CONFIRM` 後 queue 計數增加、遠端目錄可見複製檔。
- 最少測試案例範例：有 SN、空 SN、同日連續兩批次。

### 常見卡點

- 直接在 UI 執行緒做重 I/O，導致卡頓。
- 忽略 thread-safe，導致流水號重複。
- 只測「有 SN」不測「空 SN」與邊界值。
- 忘記確認資料夾層級，誤以為檔名正確就代表流程正確。

---

## 第 5 章：海康 SDK 與硬體整合（HikCamera）

### 章節目標

- 讓學員能看懂 SDK 連線與取像邏輯。
- 面對「找不到相機 / 取幀逾時」有第一層排障能力。

### 授課重點

- Runtime 路徑準備與 `MvImport` 載入。
- 列舉設備、IP 比對、fallback 索引策略。
- 軟體觸發與 `MV_CC_GetOneFrameTimeout`。
- pixel format 轉換與 fallback。
- 預覽執行緒與拍照鎖競爭控制。

### 教師示範

- 刻意輸入錯誤 IP，示範相機如何進入 fallback（依列舉順序）與 log 訊息判讀。
- 示範 `HIK_SDK_AVAILABLE=False` 的行為，讓學員知道缺 SDK 時系統會怎麼退化。
- 示範 `Grab timeout` 太短造成 NG，再逐步上調觀察改善。
- 解析一次連線失敗與取幀失敗的 log 差異，訓練學員快速定位故障層級。

### 實作任務

- 任務 1：提供三份故障 log（連線失敗、取幀逾時、像素轉換 fallback），請學員分類與標註原因。
- 任務 2：每組提出故障處理 SOP（檢查順序需含軟體與硬體面）。
- 任務 3：模擬「某台相機常 NG」情境，提出最小調整方案與驗證方法。

### 驗收標準

- 能正確區分「SDK 載入問題 / 裝置列舉問題 / 取幀逾時問題」。
- 能提出至少 3 個故障點，且處理順序合理（先快檢再深查）。
- SOP 必須包含可重現的檢查步驟與判斷條件（不是口號式描述）。

**參考答案（教師用）**
- 區分依據範例：
  - SDK 載入問題：`HIK_SDK_AVAILABLE=False`、匯入/Runtime 路徑錯誤。
  - 列舉問題：`EnumDevices` 失敗或列舉台數不足。
  - 取幀問題：`GetOneFrameTimeout` 逾時、需調 `grab timeout/stagger`。
- 建議 SOP 順序：先看 log 分層 -> 檢查線材/供電/網段 -> 檢查 IP/列舉 -> 調參數 -> 必要時再改程式。
- 合格答案需包含「可觀察指標」：例如 NG 次數、逾時比例、單次最慢耗時。

### 常見卡點（教師提示）

- 學員常直接改程式，卻沒先用 log 判斷故障層級。
- 容易把 fallback 當成正確配對，忽略實體相機對應關係錯置風險。

---

## 第 6 章：UI 操作流與執行緒安全

### 章節目標

- 理解 Tkinter 在本專案如何避免跨執行緒更新造成問題。
- 能新增小型 UI 功能（不破壞既有流程）。

### 授課重點

- `root.after()` 將背景結果切回主執行緒更新畫面。
- 預覽影像縮放與快取邏輯（降低重複 resize 成本）。
- 拍照前停預覽、確認後恢復預覽的設計意圖。
- 快捷鍵與全螢幕切換。

### 教師示範

- 示範正確做法：背景執行緒回呼資料，UI 透過 `root.after()` 更新畫面。
- 示範錯誤做法：背景執行緒直接改 UI（僅示意，不保留），說明可能造成的閃退與 race condition。
- 示範預覽縮圖快取命中與未命中時的效能差異。
- 示範拍照前停預覽、確認後恢復預覽的觀察點（CPU 與流暢度變化）。

### 實作任務

- 任務 1：新增「最近一次確認存檔時間」欄位，僅在 `CONFIRM` 成功後更新。
- 任務 2：新增一個小提示文字（例如目前是否為預覽中），練習狀態同步。
- 任務 3：為新欄位設計 2 個例外情境（尚未確認、重拍後不應更新）並測試。

### 驗收標準

- 新欄位在正確時機更新，不會在 START 或 RETAKE 誤觸發。
- 不發生跨執行緒 UI 更新錯誤，程式可穩定連續操作 10 分鐘以上。
- 既有按鈕行為與快捷鍵（空白/Enter/Esc）不受影響。

**參考答案（教師用）**
- 更新時機正解：僅 `CONFIRM` 成功後更新「最近一次確認存檔時間」。
- 執行緒安全正解：UI 更新必須走 `root.after()` 或主執行緒 callback，不可在 worker 直接改 widget。
- 回歸測試清單：START/RETAKE/CONFIRM、切頁、快捷鍵、全螢幕切換都要覆蓋。

### 常見卡點（教師提示）

- 學員常把狀態變數放在不一致位置，導致 UI 顯示與實際流程不同步。
- 容易忽略 `after` 時序，造成更新晚到或覆蓋。

---

## 第 7 章：NG 錯誤排除與效能調校

### 章節目標

- 建立標準化排障流程，避免靠猜。
- 會以數據（成功率、耗時）驗證調整是否有效。

### 授課重點

- 參數調整順序：
  1. `Stagger`
  2. `Grab timeout`
  3. `Resize/Quality`
- 指標定義：
  - NG 發生率
  - 平均拍攝耗時
  - 上傳佇列峰值
- 最小實驗法：每次僅改 1–2 參數，固定條件連拍 20–30 次。

### 教師示範

- 先定義基準組（目前設定）再定義調校組（只改 1–2 參數），示範可比性實驗。
- 示範連拍 20 次並記錄：NG 次數、平均耗時、最慢耗時、佇列峰值。
- 示範如何解讀「速度變快但 NG 上升」與「穩定提升但節拍下降」的取捨。
- 示範產出一頁式調校報告（背景、假設、調整、結果、決策）。

### 實作任務

- 任務 1：每組選一個調校目標（降低 NG 或縮短耗時），提出可驗證假設。
- 任務 2：執行至少兩輪實驗（基準組/調校組），每輪至少 20 次拍照。
- 任務 3：提交一頁排障紀錄（問題、假設、調整、數據、結論、後續建議）。

### 驗收標準

- 結論必須由數據支持，且有對照組可比較。
- 能指出「調整有效」或「無效」的明確判斷門檻。
- 能說明副作用與風險（例如速度提升但穩定性下降）。

**參考答案（教師用）**
- 對照組最小要求：基準組與調校組各 >= 20 次，場景條件一致。
- 判斷門檻範例：NG 率下降 >= 30% 且平均耗時增加 <= 10% 視為可接受改善（可依現場調整）。
- 典型可接受結論格式：改 `stagger 50->100` 後 NG 由 20% 降到 5%，平均耗時 +6%，採用。

### 常見卡點（教師提示）

- 常見錯誤是同時改太多參數，導致結果不可歸因。
- 有些學員只看平均值，忽略尖峰與最差情況（現場常由最差值決定體感）。

---

## 第 8 章：打包部署與交付演練

### 章節目標

- 讓學員能完成從程式到可執行檔的交付流程。
- 建立交付前檢查清單，降低現場部署失敗率。

### 授課重點

- `build_onefile.bat` 流程與前置條件（venv、pip、pyinstaller）。
- 為何目標機需先裝 MVS Runtime。
- 交付檢查清單：
  - EXE 可啟動
  - settings 可寫入
  - 相機可連線
  - 拍照/確認/上傳流程通過
  - log 可用於排障

### 教師示範

- 在乾淨環境執行 `build_onefile.bat`，逐步解說每段檢查（venv、pip、pyinstaller）。
- 示範打包後首次啟動檢查：設定可寫、相機可連、基本拍照可跑。
- 示範「缺 MVS Runtime」時的失敗症狀，並說明交付文件要如何提前預防。
- 示範交付包內容建議（exe、設定範例、安裝前置、快速排障指南）。

### 實作任務

- 任務 1：學員獨立完成一次打包，輸出執行檔與版本標記說明。
- 任務 2：在另一台測試機做 smoke test（啟動、拍照、確認、複製路徑檢查）。
- 任務 3：提交交付紀錄單，內容包含環境、步驟、結果、問題與處置。

### 驗收標準

- 可在指定機器完成端到端流程，且無阻塞性錯誤。
- 交付紀錄資訊完整，可讓第三人重現相同步驟。
- 能口頭說明至少 3 個部署風險與對應預防措施。

**參考答案（教師用）**
- 端到端最低標準：啟動成功 -> 相機連線 -> START/CONFIRM 成功 -> 本機落檔 -> 遠端複製可見。
- 交付紀錄必備欄位：版本、環境、步驟、結果、異常、處置、回滾方式。
- 部署風險標準答案示例：
  - 目標機未裝 MVS Runtime -> 交付前檢查清單加安裝步驟。
  - 網路路徑權限不足 -> 先做寫入 smoke test。
  - 與開發機 Python/驅動差異 -> 固定版本與相容性矩陣。

### 常見卡點（教師提示）

- 學員容易在開發機成功就當完成，忽略目標機差異（權限、Runtime、網路路徑）。
- 常漏掉「回滾方案」與版本辨識，導致現場出問題難以快速切換。

---

## 評量設計（建議）

### A. 形成性評量（課中）

- 章節小測（口頭問答）
- 程式閱讀闖關（指出功能在哪個檔案）
- 小改動任務（不破壞既有流程）

### B. 總結性評量（課後）

- 任務：新增一個「可設定、可驗證」的小功能  
  例：新增「拍照批次備註欄位」，寫入檔名或 metadata log。
- 評分面向：
  - 正確性（40%）
  - 穩定性（30%）
  - 可維護性（20%）
  - 文件與說明（10%）

---

## 教師備課清單

- 開課前：
  - 準備一套真機環境 + 一套 mock 環境
  - 確認 MVS 與 Python 版本
  - 準備一份「故障劇本」（IP 錯誤、網路慢、路徑不可寫）
- 課中：
  - 每章至少一次 live demo
  - 讓學員先預測結果，再執行驗證
- 課後：
  - 收斂常見錯誤，更新 FAQ
  - 將優秀學員作業納入下期教材範例

---

## 附錄：2 天課程排程範例

### Day 1

- 09:30–10:30 第 1 章
- 10:40–11:40 第 2 章
- 13:30–14:30 第 3 章
- 14:40–16:10 第 4 章（含實作）
- 16:20–17:20 第 5 章

### Day 2

- 09:30–10:30 第 6 章
- 10:40–12:00 第 7 章（含調校實驗）
- 13:30–15:00 第 8 章（打包與部署）
- 15:10–16:30 總結評量與 code review
- 16:30–17:00 Q&A 與個別回饋

---

## 後續可擴充教材（可選）

- 進階 A：重構 `logger`（避免重複 handler）
- 進階 B：加入自動化測試（mock camera 下的流程測試）
- 進階 C：上傳模組改為可切換 copy / API upload 策略

