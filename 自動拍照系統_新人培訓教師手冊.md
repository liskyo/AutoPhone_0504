# 自動拍照系統（AutoPhote）新人培訓教師手冊

> 適用對象：新進軟體工程師（Python / 桌面系統 / 工業相機整合）  
> 建議課程型態：2 天工作坊（可拆為 8 堂 60–90 分鐘）  
> 教學目標：讓新人可在短時間內理解系統架構、能獨立除錯、可執行小型功能開發與交付

---

## 教師使用說明

- 本手冊以「先懂流程、再看模組、最後動手改」為主線。
- 每章都包含：**章節目標、授課重點、示範操作、實作任務、驗收標準、常見卡點**。
- 若學員程度不一，建議採「核心必修 + 進階選修」：
  - 必修：第 1–6 章
  - 進階：第 7–8 章

---

## 章節總覽（8 章）

1. 系統全貌與業務流程
2. 專案結構與啟動生命週期
3. 設定系統與環境切換（真機/模擬）
4. 拍照核心流程（CaptureManager）
5. 海康 SDK 與硬體整合（HikCamera）
6. UI 操作流與執行緒安全
7. NG 錯誤排除與效能調校
8. 打包部署與交付演練

---

## 第 1 章：系統全貌與業務流程

### 章節目標

- 讓學員知道系統在產線中的角色與價值。
- 建立「START → REVIEW → CONFIRM → Upload」完整心智模型。

### 授課重點

- AutoPhote 是多相機拍照工作站，不是單純拍照程式。
- 操作流程：
  - `START`：觸發拍照並暫存（不立即落檔）
  - `CONFIRM`：正式存檔 + 進入上傳佇列
  - `RETAKE`：捨棄暫存結果
- 背景上傳與前台操作解耦，降低操作卡頓。

### 教師示範（10–15 分）

- 啟動程式，示範一次標準拍照流程。
- 特別指出 UI 上每個按鈕與相機狀態（READY/GO/OK/NG/CHECK）的對應關係。

### 實作任務（學員）

- 口頭與白板畫出系統資料流（至少包含 UI、Capture、Queue、Upload）。

### 驗收標準

- 能正確描述為何 `START` 後不直接存檔。
- 能說出 queue 在架構裡的用途。

### 常見卡點（教師提示）

- 學員容易以為「上傳」是 HTTP；本系統實作是 copy 到遠端路徑。

---

## 第 2 章：專案結構與啟動生命週期

### 章節目標

- 能快速定位需求應修改的檔案。
- 理解 `main.py` 如何組裝整個系統。

### 授課重點

- 目錄職責：
  - `ui/`：介面與事件
  - `services/`：業務流程（拍照、上傳、檔案）
  - `hardware/`：實體相機/模擬相機
  - `config.py`：設定載入與套用
- `main.py` 流程：
  - 建立資料夾
  - 建立共享 queue
  - 建立 `CaptureManager` / `UploadManager`
  - 建立 UI 與 callback 注入
  - 啟動預覽與上傳執行緒
  - 關閉時資源釋放

### 教師示範

- 帶學員走讀 `main.py`，逐段說明每個初始化順序為何不能任意交換。

### 實作任務

- 請學員在不改功能前提下，畫出 `main.py` 的呼叫順序圖。

### 驗收標準

- 能說出為什麼 `tk.Tk()` 要在 UI 初始化前建立。
- 能說出 `capture_mgr.shutdown()` 的必要性。

---

## 第 3 章：設定系統與環境切換（真機/模擬）

### 章節目標

- 學會調整 `settings.json` 與理解哪些設定可即時生效。
- 會切換真機與 mock 環境做開發。

### 授課重點

- `config.py`：
  - `DEFAULT_SETTINGS`
  - `load_settings()` / `save_settings()` / `reload_settings()`
  - `USE_REAL_CAMERA` 切換
- 可即時套用：stagger、grab timeout、品質、縮放、路徑。
- 需重啟：相機數量、IP（涉及重新連線與 UI 網格重建）。

### 教師示範

- 在設定頁調整 `Stagger`、`Grab timeout` 並立即測試。
- 切到 mock 模式跑一次，證明無硬體也可進行功能驗證。

### 實作任務

- 學員建立一組「穩定優先」與「速度優先」設定檔（文字描述即可）。

### 驗收標準

- 能解釋 `reload_settings()` 為什麼不等同完整重啟。

---

## 第 4 章：拍照核心流程（CaptureManager）

### 章節目標

- 理解拍照流程的並行模型與資料生命週期。
- 能安全修改命名規則、存檔策略或批次流程。

### 授課重點

- `initialize_cameras()`：固定長度 camera slot，確保索引與 UI 對齊。
- `trigger_batch_capture(save_now=False)`：平行提交拍照任務。
- `_capture_task()`：
  - stagger 延遲
  - grab image
  - overlay timestamp
  - 更新 UI / pending
- `confirm_save()`：將 pending 寫檔並入 upload queue。
- 命名規則：`SN_YYYYMMDD_序號` + 民國日期資料夾與 session 子資料夾。

### 教師示範

- 加入一段 debug log，顯示每台相機實際拍攝起始時間差，觀察 stagger 效果。

### 實作任務

- 修改規則：當 SN 空白時，檔名前綴從 `UNKNOWN` 改為 `NOSN`（練習小改動）。

### 驗收標準

- 功能可運作且不破壞既有流程（START/CONFIRM/RETAKE 仍正常）。

### 常見卡點

- 直接在 UI 執行緒做重 I/O，導致卡頓。
- 忽略 thread-safe，導致流水號重複。

---

## 第 5 章：海康 SDK 與硬體整合（HikCamera）

### 章節目標

- 讓學員能看懂 SDK 連線與取像邏輯。
- 面對「找不到相機 / 取幀逾時」有第一層排障能力。

### 授課重點

- Runtime 路徑準備與 `MvImport` 載入。
- 列舉設備、IP 比對、fallback 索引策略。
- 軟體觸發與 `MV_CC_GetOneFrameTimeout`。
- pixel format 轉換與 fallback。
- 預覽執行緒與拍照鎖競爭控制。

### 教師示範

- 刻意輸入錯誤 IP，示範 fallback 行為與 log 判讀。

### 實作任務

- 學員閱讀 log，判斷錯誤發生在「連線階段」還是「取像階段」。

### 驗收標準

- 能說出至少 3 個與硬體整合相關的故障點與處理順序。

---

## 第 6 章：UI 操作流與執行緒安全

### 章節目標

- 理解 Tkinter 在本專案如何避免跨執行緒更新造成問題。
- 能新增小型 UI 功能（不破壞既有流程）。

### 授課重點

- `root.after()` 將背景結果切回主執行緒更新畫面。
- 預覽影像縮放與快取邏輯（降低重複 resize 成本）。
- 拍照前停預覽、確認後恢復預覽的設計意圖。
- 快捷鍵與全螢幕切換。

### 教師示範

- 示範錯誤做法：背景執行緒直接改 UI（說明風險，不建議實作）。

### 實作任務

- 新增一個 UI 顯示欄位：最近一次確認存檔時間。

### 驗收標準

- 新欄位可更新，且不影響原有按鈕流程與預覽穩定度。

---

## 第 7 章：NG 錯誤排除與效能調校

### 章節目標

- 建立標準化排障流程，避免靠猜。
- 會以數據（成功率、耗時）驗證調整是否有效。

### 授課重點

- 參數調整順序：
  1. `Stagger`
  2. `Grab timeout`
  3. `Resize/Quality`
- 指標定義：
  - NG 發生率
  - 平均拍攝耗時
  - 上傳佇列峰值
- 最小實驗法：每次僅改 1–2 參數，固定條件連拍 20–30 次。

### 教師示範

- 實際跑一輪「基準值 vs 調校值」對比，示範如何解讀結果。

### 實作任務

- 學員提交一頁排障紀錄（問題、假設、調整、結果、結論）。

### 驗收標準

- 結論能對應到觀察數據，不是主觀描述。

---

## 第 8 章：打包部署與交付演練

### 章節目標

- 讓學員能完成從程式到可執行檔的交付流程。
- 建立交付前檢查清單，降低現場部署失敗率。

### 授課重點

- `build_onefile.bat` 流程與前置條件（venv、pip、pyinstaller）。
- 為何目標機需先裝 MVS Runtime。
- 交付檢查清單：
  - EXE 可啟動
  - settings 可寫入
  - 相機可連線
  - 拍照/確認/上傳流程通過
  - log 可用於排障

### 教師示範

- 在乾淨環境做一次完整打包與 smoke test。

### 實作任務

- 學員獨立完成打包並提交測試結果。

### 驗收標準

- 可在指定機器完成端到端拍照流程。

---

## 評量設計（建議）

### A. 形成性評量（課中）

- 章節小測（口頭問答）
- 程式閱讀闖關（指出功能在哪個檔案）
- 小改動任務（不破壞既有流程）

### B. 總結性評量（課後）

- 任務：新增一個「可設定、可驗證」的小功能  
  例：新增「拍照批次備註欄位」，寫入檔名或 metadata log。
- 評分面向：
  - 正確性（40%）
  - 穩定性（30%）
  - 可維護性（20%）
  - 文件與說明（10%）

---

## 教師備課清單

- 開課前：
  - 準備一套真機環境 + 一套 mock 環境
  - 確認 MVS 與 Python 版本
  - 準備一份「故障劇本」（IP 錯誤、網路慢、路徑不可寫）
- 課中：
  - 每章至少一次 live demo
  - 讓學員先預測結果，再執行驗證
- 課後：
  - 收斂常見錯誤，更新 FAQ
  - 將優秀學員作業納入下期教材範例

---

## 附錄：2 天課程排程範例

### Day 1

- 09:30–10:30 第 1 章
- 10:40–11:40 第 2 章
- 13:30–14:30 第 3 章
- 14:40–16:10 第 4 章（含實作）
- 16:20–17:20 第 5 章

### Day 2

- 09:30–10:30 第 6 章
- 10:40–12:00 第 7 章（含調校實驗）
- 13:30–15:00 第 8 章（打包與部署）
- 15:10–16:30 總結評量與 code review
- 16:30–17:00 Q&A 與個別回饋

---

## 後續可擴充教材（可選）

- 進階 A：重構 `logger`（避免重複 handler）
- 進階 B：加入自動化測試（mock camera 下的流程測試）
- 進階 C：上傳模組改為可切換 copy / API upload 策略

