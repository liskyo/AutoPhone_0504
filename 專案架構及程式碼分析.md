# AutoPhote 工業相機自動化拍攝系統 - 專案架構及程式碼分析

## 1. 專案概述

AutoPhote 是一套工業現場用的多相機拍照系統，提供即時預覽、人工確認、規則命名存檔與背景上傳。

核心特色：

- 支援多相機並行拍攝（真實相機 / 模擬相機）
- 拍照採「先預覽、再確認存檔」流程
- 檔名與資料夾依 `SN_日期_流水號` 規則建立
- 存檔後自動進入背景上傳佇列，失敗可重試

---

## 2. 目錄結構

```text
AutoPhote/
├── main.py
├── config.py
├── settings.json
├── build_onefile.bat
├── hardware/
│   ├── hik_camera.py
│   └── mock_camera.py
├── services/
│   ├── capture_manager.py
│   ├── upload_manager.py
│   └── file_service.py
├── ui/
│   └── dashboard.py
└── utils/
    ├── image_utils.py
    └── logger.py
```

---

## 3. 模組職責說明

### 3.1 `main.py`（系統組裝層）

- 啟動程式與 UI 主迴圈
- 建立 `CaptureManager`、`UploadManager`
- 綁定 UI 操作事件：`START`、`CONFIRM`、`RETAKE`
- 負責關機時釋放資源（停止預覽、關閉上傳執行緒）

### 3.2 `config.py`（設定管理）

- 載入/儲存 `settings.json`
- 提供全域設定（相機數量、IP、畫質、路徑、重試次數）
- exe 模式下使用 `sys.executable` 旁的設定檔路徑，確保設定可持久化

### 3.3 `hardware/hik_camera.py`（真實相機驅動）

- 載入 MVS SDK（含 runtime DLL 路徑準備）
- 實作連線、取像、預覽串流
- `grab_image()` 透過 SDK 取 frame 並轉成 PIL Image

### 3.4 `hardware/mock_camera.py`（模擬相機）

- 無實機時可生成測試影像
- 保持與真實相機一致的介面（`connect`, `disconnect`, `grab_image`）

### 3.5 `services/capture_manager.py`（拍照流程核心）

- 初始化所有相機
- 使用 `ThreadPoolExecutor` 並行拍照
- 管理暫存拍照結果（待確認）
- 確認後執行存檔與佇列上傳
- 實作檔名/資料夾規則與流水號管理

### 3.6 `services/upload_manager.py`（背景上傳）

- 以 queue 方式接收待上傳檔案
- 背景執行 copy 到遠端路徑
- 含重試機制（`MAX_RETRIES`, `UPLOAD_RETRY_DELAY`）

### 3.7 `services/file_service.py`（檔案服務）

- 建立資料夾
- 儲存圖片（JPEG 失敗時 fallback PNG）
- 複製/移動/刪除檔案

### 3.8 `ui/dashboard.py`（操作介面）

- 監控頁（相機預覽、狀態、控制按鈕）
- 設定頁（相機數量、IP、品質、儲存路徑）
- 監控頁支援額外相機卷軸顯示與滑鼠滾輪捲動
- 提供 SN 欄位，供檔名與資料夾命名使用

---

## 4. 系統運行的拍照原理

### 4.1 拍照操作流程

1. 使用者輸入 SN，按下 `START`
2. 系統先停止預覽串流，觸發多相機同步拍照
3. 每台相機取像後先顯示在 UI（Review 狀態），先不落檔
4. 使用者按下 `CONFIRM` 才正式存檔
5. 存檔成功的影像放入上傳佇列，背景上傳到遠端

### 4.2 影像處理流程

每張影像在拍照流程中的處理順序：

1. 相機回傳原始影像
2. 疊加時間戳（`overlay_timestamp`）
3. UI 顯示預覽（縮圖）
4. 確認後依 `RESIZE_RATIO` 做輸出縮放
5. 以 `JPEG_QUALITY` 儲存到本機目標資料夾

### 4.3 命名與資料夾規則

- 資料夾：`序號_日期`  
  例：`SN001_20260130`
- 檔名：`序號_日期_流水號`  
  例：`SN001_20260130_001.jpg`

規則細節：

- 流水號三位數（001 起）
- 若發現同名檔存在，自動往後加一避免覆蓋
- 每輪拍攝確認後可重置流水號策略（依目前程式設定）

### 4.4 多相機並行機制

- `capture_manager` 使用 thread pool 並行觸發各相機
- 每台相機拍照在獨立 worker 執行，降低互相等待
- UI 更新透過 callback 回主執行緒，避免 Tkinter 執行緒問題

---

## 5. 打包與部署重點

- 提供 `build_onefile.bat` 一鍵打包單檔 exe
- 打包前會嘗試關閉舊的 `AutoPhote.exe`，避免覆蓋失敗
- 真實相機模式需已安裝 MVS，且 runtime DLL 可被程式找到

---

## 6. 維運建議

- 正式環境固定 Python/SDK 版本，避免驅動相容性問題
- 建議保留 `system.log` 方便現場追查連線與上傳失敗原因
- 設定頁儲存後需重新啟動程式，讓新設定完整生效

---

## 7. SDK 用途說明（MVS SDK）

在本系統中，SDK 是「程式與工業相機之間的溝通層」。

主要用途：

- 裝置枚舉：列出目前可連線相機
- 相機連線：建立控制與影像資料通道
- 參數控制：觸發模式、曝光、增益、像素格式
- 軟體觸發：發送 trigger 指令拍照
- 影像取回：從相機拉回 raw frame
- 像素轉換：Bayer/Mono 轉 RGB，供 UI 顯示與存檔
- 串流預覽：連續抓圖回傳預覽
- 錯誤碼回報：提供診斷與重試依據

在程式中的對應：

- `hardware/hik_camera.py`：SDK 呼叫入口（連線、取像、預覽）
- `MvCameraControl_class.py`：Python 封裝層（ctypes 介面）
- `MvCameraControl.dll`：底層執行核心

---

## 8. SDK 呼叫流程圖（按 START 到取得影像）

```text
[使用者按下 START]
        |
        v
[UI: dashboard.handle_snap()]
        |
        v
[CaptureManager.trigger_batch_capture(save_now=False)]
        |
        v
[ThreadPoolExecutor 並行啟動每台相機 _capture_task]
        |
        v
[HikCamera.grab_image()]
  ├─ MV_CC_SetCommandValue("TriggerSoftware")
  ├─ MV_CC_GetOneFrameTimeout(...)
  ├─ MV_CC_ConvertPixelType(...)  (必要時)
  └─ 轉成 PIL Image
        |
        v
[overlay_timestamp()]
        |
        v
[update_cam_image_callback -> UI Canvas 預覽]
        |
        v
[暫存 pending_captures，等待使用者 CONFIRM]
        |
        v
[使用者按下 CONFIRM]
        |
        v
[CaptureManager.confirm_save()]
        |
        v
[依命名規則存檔 + 加入 upload_queue]
        |
        v
[UploadManager 背景上傳遠端]
```
